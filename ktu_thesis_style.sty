% All styles of the document

\usepackage[utf8]{inputenc}
\usepackage{mfirstuc} % For title capitalization
\MFUnocap{the}\MFUnocap{and} %Not capitalising "the" and "and"


% Font: Times New Roman, 12 pt
\usepackage[T1]{fontenc}
\usepackage{mathptmx}
\usepackage{graphicx}

% Paper size and margins: A4 (210 x 297 mm), margins: left – 30 mm, right – 10 mm, top and bottom – 20 mm.
\usepackage[a4paper, left=30mm, right=10mm, top=20mm, bottom=20mm]{geometry}

% headers and footers
\usepackage{fancyhdr}
% Clear the header and footer
\fancyhead{}
\fancyfoot{}

% Puslapių numeravimas: Puslapiai numeruojami nuosekliai, skaičiuojant nuo antraštinio lapo, o pradedami rašyti nuo turinio. Puslapio numeris rašomas apatinės paraštės dešinėje pusėje arabiškais skaitmenimis be taškų.

%customize page numbering on right hand side
\fancypagestyle{plain}{% Redefining plain page style
  \fancyhf{} %clear all header and footer fields
  \fancyfoot[RO]{\thepage}
}%
\fancyhf{} %clear all header and footer fields
\fancyfoot[RO]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\pagestyle{fancy}% Fancy page style




% Line spacing – 1,15
\usepackage{setspace}
\setstretch{1.15}

% Tarpas tarp teksto pastraipų: Atstumas prieš pastraipą, po pastraipos ir tarp jų – 10 pt, lentelėse – 3 pt.
\setlength{\parskip}{10pt}

% Teksto lygiuotė: Abipusė, pirmos pastraipos eilutės įtrauka nedaroma (nuimama įtrauka):
\setlength{\parindent}{0em}


% Teksto skirstymas į dalis ir jų numeracija: Skyrius pradedamas naujame puslapyje
\usepackage{titlesec}
\titleformat{\section}
	{\normalfont\fontsize{12}{10} %fonto dydis ir tarpai po/pries sekcija
	\bfseries} 
	 {\thesection.\quad} % taškas po sekcijos
	{0em}{} 
\titleformat{\subsection}
	{\normalfont\fontsize{12}{10} %fonto dydis ir tarpai po/pries sekcija
	\bfseries} 
	 {\thesubsection.\quad} % taškas po sekcijos
	{0em}{} 
\titleformat{\subsubsection}
	{\normalfont\fontsize{12}{10} %fonto dydis ir tarpai po/pries sekcija
	\bfseries} 
	 {\thesubsubsection.\quad} % taškas po sekcijos
	{0em}{} 

\usepackage{hyperref}
%\hypersetup{
%    colorlinks=true,
%    linkcolor=cyan,
%    filecolor=cyan,      
%    urlcolor=cyan
%    %citecolor=black, %Comment out while working, easier to see cite
%}

\hypersetup{
    colorlinks=False,
   % citecolor=black,
}


% Section/subsection page breaks
\titleclass{\section}{top}
\newcommand\sectionbreak{\clearpage}


\usepackage{enumitem} %numbered lists and bullet points


% \RequirePackage{LTPlius}  % Nustatymų sulietuvinimas, iš VU šablono: https://github.com/LIKS/course_work_template_vu_mif_cs1

% \usepackage[lithuanian]{babel}
\usepackage[english]{babel}



% for tables
\usepackage{booktabs}
\usepackage{tabularx}


% Captions formatting
% Caption, Title of the figure: Font size 11 pt., line spacing – 1.15, spacing before and after a title – 10 pt., centered
% Title of the table: For the titles of the tables (number and a word table are written in bold font).
\usepackage[%justification=centering, %centering captions
	font={small, %using small font (if doc is 12pt, small is 11pt)
		stretch=1.15 %line spacing – 1.15
		}, 
	tableposition=top,
	figureposition=bottom,
	labelfont=bf % Bold font for "Figure" and "Table"
	]{caption} 
	
% \captionsetup[figure]{name=Fig.} %If need to change from Figure to Fig. as in template example

\captionsetup[table]{justification=raggedright} 
\captionsetup[figure]{justification=centering} 

\usepackage{subcaption}


% Section number in tables and figures
\usepackage{chngcntr}
\counterwithin{table}{section}
\counterwithin{figure}{section}

% If charts and tables shouldn't be places before the section
% \usepackage[section]{placeins}


\usepackage{amsmath}

% For charts
\usepackage{tikz} 

% Bibliography
% \usepackage[style=iso-authoryear]{biblatex}
%\usepackage[style=iso-numeric]{biblatex}

\usepackage[numbers]{natbib}
\bibliographystyle{unsrtnat} % Sorting by appearance
\usepackage{csquotes}
% \setcitestyle{authoryear,open={(},close={)}} %Citation-related commands
% \usepackage[doi=false,isbn=false,url=false,eprint=false]{biblatex}




% For the table in multiple pages:
\usepackage{longtable}

%% To use \paragraph as subsubsubsection (one even deeper level section)
%\titleformat{\paragraph}
%{\normalfont\normalsize\bfseries}{\theparagraph}{1em}{}
%\titlespacing*{\paragraph}
%{0pt}{3.25ex plus 1ex minus .2ex}{1.5ex plus .2ex}
%
%\setcounter{secnumdepth}{4} % To enable numbering on "paragraph"
%\setcounter{tocdepth}{4} % To make "paragraph" appear in the table of content


% Auto title case for sections and subsections, but throws errors
%\usepackage{sectsty}
%\usepackage{titlecaps}
%\allsectionsfont{\titlecap}


\usepackage{setspace}

% Testing usebib if it's possible to cite the title
%\usepackage{usebib}


\usepackage{longtable} % For the table on multiple pages
\usepackage{lscape} % To rotate the table

% To remove justification from tables: https://tex.stackexchange.com/questions/66123/how-do-i-set-right-alignment-in-a-longtable-column-with-a-fixed-width:
\usepackage{array} 
\newcolumntype{R}{>{\raggedright\arraybackslash}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{L}[1]{>{\raggedleft\arraybackslash}p{#1}}
%\newcolumntype{\Rp}[1]{>{\raggedright\arraybackslash}p{#1}}


% Should do title case for sections, but messes things up
%\usepackage{sectsty}
%\usepackage{titlecaps}
%\allsectionsfont{\titlecap}

%Temporal, to figure out the page width (to later use in the charts), use:
%\usepackage{layouts} 
%textwidth in cm: \printinunitsof{cm}\prntlen{\textwidth} # Add somewhere in the document to print it


\numberwithin{equation}{section} %Leave if equations should be numbered as section_number.equation_number, comment out otherwise
%For timeline chart
\usepackage{tikz}
\usetikzlibrary{decorations.pathreplacing,positioning, arrows.meta}

% emoji support
%\usepackage{emoji}


%\usepackage{changepage} % For the adjustwidth

\usepackage{tabularx} % For tables not to go out of page margins


\usepackage{lastpage} % To get the last page for the text input


\usepackage{afterpage} % for the figures in appendix to appear after subsection

\usepackage{pgfplots}



%----------------Adjusting list of tables and list of figures----------
\usepackage{tocloft} % to add Fig. and Table to list of figures and list of tables


% Modify the format of the figure numbers in the list of figures
\newpage
\renewcommand{\cftfigpresnum}{\bfseries{Fig.~}} % put before the number
\renewcommand{\cftfigaftersnum}{. }  % put after the number
% Adjust spacing
\settowidth{\cftfignumwidth}{\textbf{Fig.~999.\quad}}


% Modify the format of the table numbers in the list of tables
\renewcommand{\cfttabpresnum}{\bfseries{Table~}}
\renewcommand{\cfttabaftersnum}{. }
% Adjust spacing
\settowidth{\cfttabnumwidth}{\textbf{Table~999.\quad}}

% Title format for the list of tables, figures and content
\renewcommand\cftloftitlefont{\normalfont\fontsize{12}{10}\bfseries}
\renewcommand\cftlottitlefont{\normalfont\fontsize{12}{10}\bfseries}
\renewcommand\cfttoctitlefont{\normalfont\fontsize{12}{10}\bfseries}

% Adjust indentation of the list of tables and list of figures
\setlength{\cfttabindent}{0pt} % No indentation for list of tables
\setlength{\cftfigindent}{0pt} % No indentation for list of figures


% If all table captions needs to be aligned left:
\captionsetup[table]{singlelinecheck=false, justification=raggedright}

